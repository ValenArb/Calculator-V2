import { db } from './config';

// Firestore service - Currently only used for error codes and user management
// Project-related functionality has been removed for rebuild
export const firestoreService = {
  // Placeholder for future project functionality
  // This will be rebuilt with new database architecture
};